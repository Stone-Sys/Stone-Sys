# Visit https://github.com/lowlighter/metrics#-documentation for full reference
name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{cron: "0 * * * *"}]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          # The following scopes are required:
          #  - public_access (default scope)
          # The following additional scopes may be required:
          #  - read:org      (for organization related metrics)
          #  - read:user     (for user related data)
          #  - read:packages (for some packages related data)
          #  - repo          (optional, if you want to include private repositories)
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          template: terminal
          base: header, activity, community, repositories, metadata
          base_hireable: yes
          config_timezone: America/Argentina/Buenos_Aires
          plugin_achievements: yes
          plugin_achievements_display: detailed
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: C
          plugin_habits: yes
          plugin_habits_charts_type: classic
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_from: 200
          plugin_habits_languages_limit: 8
          plugin_habits_languages_threshold: 0%
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_analysis_timeout_repositories: 7.5
          plugin_languages_categories: markup, programming
          plugin_languages_colors: github
          plugin_languages_ignored: javascript, html, css
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 14
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%

          # Inyección de Estética Kitty / Tokyonight
          extras_css: |
            /* Fondo y transparencia estilo Kitty */
            .terminal {
              background: rgba(26, 27, 38, 0.75) !important;
              color: #a9b1d6 !important;
              font-family: 'JetBrainsMono Nerd Font Mono', monospace !important;
              border: 1px solid #414868 !important;
              padding: 10px !important;
              border-radius: 8px !important;
            }
            /* Barra superior de la ventana */
            .terminal-header {
              background: #16161e !important;
              color: #7aa2f7 !important;
              border-bottom: 1px solid #414868 !important;
            }
            /* Colores de texto (basados en tus color4 y color5) */
            h1, h2, h3, .title { color: #bb9af7 !important; }
            .field svg, .field i { fill: #7aa2f7 !important; color: #7aa2f7 !important; }
            b, strong { color: #7dcfff !important; }
            
            /* Botones de ventana */
            .terminal-button.close { background: #f7768e !important; }
            .terminal-button.minimize { background: #e0af68 !important; }
            .terminal-button.maximize { background: #9ece6a !important; }

          # Forzar renderizado en alta resolución
          config_display: large
